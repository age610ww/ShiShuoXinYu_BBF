# 世说新语 · 人物与故事索引（多 sheet 版）

将以下文件置于同一目录：
- `index.html`
- `shishuoxinyu.xlsx`  ← 支持多工作表，程序会合并所有 sheet 的行

上传到 GitHub 仓库后，在 Settings → Pages 启用 Pages（main / root），即可通过
`https://<你的用户名>.github.io/<仓库名>/` 访问。
